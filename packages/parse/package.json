{
  "name": "@ledgerline/parse",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL DDL \u2192 the declared schema, through PostgreSQL's own grammar.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --conditions=source --test 'test/**/*.test.ts'",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@ledgerline/model": "workspace:*",
    "@pgsql/parser": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
